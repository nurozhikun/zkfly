import 'package:flutter/material.dart';
import 'package:zkfly/styles/index.dart';

class AppTheme {
  static ThemeData teal = ThemeData(
    brightness: Brightness.light,
    splashColor: Colors.transparent,
    highlightColor: Colors.transparent,
    primarySwatch: AppColor.tealColor,
  );

  static ThemeData indigo = ThemeData(
    brightness: Brightness.light,
    splashColor: Colors.transparent,
    highlightColor: Colors.transparent,
    primarySwatch: AppColor.indigoColor,
  );

  static ThemeData pink = ThemeData(
    brightness: Brightness.light,
    splashColor: Colors.transparent,
    highlightColor: Colors.transparent,
    // Swatch样品
    primarySwatch: AppColor.pinkColor,
  );
}
